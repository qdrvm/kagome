#!/bin/bash

# apt update && apt upgrade -y
# apt install -y protobuf-compiler libprotobuf-dev

# rustup default stable

# # in cumulus
# cargo build --release --bin polkadot-parachain

# cargo build --release --locked --bin test-parachain

# # in polkadot
# cargo build -p test-parachain-adder-collator

# cargo build -p test-parachain-undying-collator

# cargo build -p polkadot-test-malus

# # install zombienet

# wget https://github.com/paritytech/zombienet/releases/download/v1.3.55/zombienet-linux-x64
# chmod +x zombienet-linux-x64
# cp zombienet-linux-x64 /usr/local/bin

